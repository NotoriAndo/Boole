# Boole Status

Check whether the shared Boole agent mining wrapper is available from a Codex CLI session.

Command to run:

```bash
test -x __BOOLE_ROOT__/scripts/boole-agent-mine.sh && __BOOLE_ROOT__/scripts/boole-agent-mine.sh --help
```

Summarize the wrapper path, supported runtimes, and the rule that consensus acceptance stays in verifier/canonicalizer/node replay.
